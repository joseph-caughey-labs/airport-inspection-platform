{
  "name": "@aip/http-safety",
  "version": "0.6.0",
  "private": true,
  "description": "Shared Fastify safety primitives: sanitized error + 404 handlers, body-size cap constant. Pulls every service onto the same input-safety posture.",
  "license": "SEE LICENSE IN ../../LICENSE",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@aip/shared-contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "fastify": "^5.0.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  },
  "peerDependencies": {
    "fastify": "^5.0.0"
  }
}

{
  "name": "@aip/incident-service",
  "version": "0.4.0",
  "private": true,
  "description": "Incident lifecycle CRUD and state machine. This PR adds the shell with an empty /incidents listing; full lifecycle lands in Phase 4 (T-401..T-404).",
  "license": "SEE LICENSE IN ../../LICENSE",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@aip/auth-jwt": "workspace:*",
    "@aip/db-schema": "workspace:*",
    "@aip/logger": "workspace:*",
    "@aip/metrics": "workspace:*",
    "@aip/postgres-client": "workspace:*",
    "@aip/redis-client": "workspace:*",
    "@aip/shared-contracts": "workspace:*",
    "fastify": "^5.0.0",
    "ioredis": "^5.4.1",
    "pg": "^8.13.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}

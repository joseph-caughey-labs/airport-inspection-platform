{
  "name": "@aip/audit-service",
  "version": "0.4.0",
  "private": true,
  "description": "Append-only audit log + hash chain. This PR adds the shell with /audit/events listing; hash-chain implementation lands in T-412.",
  "license": "SEE LICENSE IN ../../LICENSE",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@aip/auth-jwt": "workspace:*",
    "@aip/logger": "workspace:*",
    "@aip/metrics": "workspace:*",
    "@aip/postgres-client": "workspace:*",
    "@aip/redis-client": "workspace:*",
    "@aip/shared-contracts": "workspace:*",
    "fastify": "^5.0.0",
    "ioredis": "^5.4.1",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}

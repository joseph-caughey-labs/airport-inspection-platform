{
  "name": "@aip/validation-engine",
  "version": "0.3.0",
  "private": true,
  "description": "Parity 10-layer validation engine. This PR adds the orchestrator scaffold + 10 layer stubs each returning { passed: true }. Real layer logic lands in T-405..T-411.",
  "license": "SEE LICENSE IN ../../LICENSE",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@aip/logger": "workspace:*",
    "@aip/metrics": "workspace:*",
    "@aip/shared-contracts": "workspace:*",
    "fastify": "^5.0.0",
    "prom-client": "^15.1.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}

{
  "name": "@aip/event-pipeline",
  "version": "0.2.0",
  "private": true,
  "description": "Event enrichment, dedup, prioritization, ordering, persistence. This PR adds the shell; real consumers/enrichment/dedup land in Phase 2 (T-205..T-208).",
  "license": "SEE LICENSE IN ../../LICENSE",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@aip/logger": "workspace:*",
    "@aip/metrics": "workspace:*",
    "@aip/postgres-client": "workspace:*",
    "@aip/redis-client": "workspace:*",
    "@aip/shared-contracts": "workspace:*",
    "fastify": "^5.0.0",
    "ioredis": "^5.4.1",
    "pg": "^8.13.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
